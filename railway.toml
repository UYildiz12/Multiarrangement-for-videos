[build]
builder = "nixpacks"
buildCommand = "python -m pip install --upgrade pip && python -m pip install -r server/requirements.txt"

[deploy]
startCommand = "PYTHONPATH=server:. uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
